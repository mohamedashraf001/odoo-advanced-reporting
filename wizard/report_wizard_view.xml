<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_report_format_wizard_form" model="ir.ui.view">
        <field name="name">report.format.wizard.form</field>
        <field name="model">report.format.wizard</field>
        <field name="arch" type="xml">
            <form string="Choose Report Format">
                <sheet>
                    <group>
                        <field name="format" widget="radio" string="Select Output Format"/>
                    </group>
                    <p class="text-muted">
                        Select **PDF** for a professional layout or **Excel** for data analysis.
                    </p>
                </sheet>
                <footer>
                    <button name="action_print" string="Print" type="object" class="btn-primary" icon="fa-print"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_report_format_wizard" model="ir.actions.act_window">
        <field name="name">Print Options</field>
        <field name="res_model">report.format.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field> </record>
</odoo>